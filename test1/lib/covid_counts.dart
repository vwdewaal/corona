import 'package:flutter/material.dart';

class covid_counts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Covid19 counts layout'),
      ),
    );
  }
}